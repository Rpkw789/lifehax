# Browserbase — the 3 real browser agents. Free tier: 3 concurrent, 1 browser-hour.
BROWSERBASE_API_KEY=

# Cloudflare AI Gateway — persona generation and findings.
# Token needs Account > Workers AI > Read. Account id: `wrangler whoami`.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_GATEWAY_ID=default
HAPPY2_MODEL=anthropic/claude-sonnet-4-5

# How many agents really drive a browser. Keep <= 3 on the free tier.
HAPPY2_REAL_AGENTS=3
